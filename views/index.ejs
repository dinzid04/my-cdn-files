<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= config.siteName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f0f2f5;
        }
    </style>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="w-full max-w-lg mx-auto p-4">
        <!-- Profile Card -->
        <div class="bg-white rounded-lg shadow-lg overflow-hidden mb-6">
            <div class="h-48 bg-cover bg-center" style="background-image: url('<%= config.profileBanner %>');"></div>
            <div class="p-6">
                <div class="flex items-center space-x-4">
                    <img class="w-24 h-24 rounded-full border-4 border-white -mt-16" src="<%= config.profilePicture %>" alt="Profile Picture">
                    <div>
                        <h1 class="text-2xl font-bold text-gray-800"><%= config.siteName %></h1>
                        <p class="text-gray-600"><%= config.authorName %></p>
                    </div>
                </div>
                <p class="mt-4 text-gray-700"><%= config.aboutText %></p>
                <a href="https://github.com/alip-jmbd/NefuSite.git" target="_blank" rel="noopener noreferrer" class="mt-4 inline-flex items-center px-4 py-2 bg-gray-800 text-white rounded-lg hover:bg-gray-900">
                    <i class="fab fa-github mr-2"></i> GitHub
                </a>
            </div>
        </div>

        <!-- Tool Selection Tabs -->
        <div class="flex border-b border-gray-200">
            <button id="cdnBtn" class="flex-1 py-3 text-center text-gray-700 font-medium border-b-2 border-transparent hover:bg-gray-100 focus:outline-none active">
                <i class="fas fa-cloud-upload-alt mr-2"></i> CDN
            </button>
            <button id="shortUrlBtn" class="flex-1 py-3 text-center text-gray-700 font-medium border-b-2 border-transparent hover:bg-gray-100 focus:outline-none">
                <i class="fas fa-link mr-2"></i> ShortURL
            </button>
        </div>

        <!-- CDN Section -->
        <div id="cdn-section" class="tool-section bg-white rounded-lg shadow-lg p-6 mt-[-1px]">
            <p class="text-center text-gray-600 mb-4">Upload files to our lightning-fast CDN.</p>
            <div id="drop-zone" class="border-2 border-dashed border-gray-300 rounded-lg p-10 text-center cursor-pointer hover:border-blue-500 bg-gray-50">
                <i class="fas fa-upload text-4xl text-gray-400"></i>
                <p class="mt-2 text-gray-600">Drag & drop a file here or click to select</p>
                <input type="file" id="file-input" class="hidden">
            </div>
            <div id="cdn-result" class="mt-4"></div>
        </div>

        <!-- ShortURL Section -->
        <div id="shorturl-section" class="tool-section bg-white rounded-lg shadow-lg p-6 mt-[-1px] hidden">
            <p class="text-center text-gray-600 mb-4">Create short, shareable links in seconds.</p>
            <div class="space-y-4">
                <input type="text" id="long-url" placeholder="Enter long URL" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                <input type="text" id="custom-code" placeholder="Custom code (optional)" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                <button id="shorten-btn" class="w-full py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 font-semibold">Shorten</button>
            </div>
            <div id="shorturl-result" class="mt-4"></div>
        </div>
    </div>
    <script src="/script.js"></script>
</body>
</html>
